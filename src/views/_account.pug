extends base

mixin navItem(link, text, icon)
  li(class=`${path === link ? 'side-nav--active' : ''}`)
    a(href=`${link}`)
      i.settings-icon.fa(aria-hidden="true" class=icon)
      p #{text}

block content
  main.main
    .user-view
      nav.user-view__menu
        ul.side-nav
          +navItem('/me', 'Settings', 'fa-cog')
          +navItem('/my-apps', 'My applications', 'fa-cloud')
          +navItem('/subscription', 'Subscription', 'fa-credit-card')
          //- +navItem('#', 'Billing', 'credit-card')

        //- - if (user.role === 'ADMIN')
        //-   .admin-nav
        //-     h5.admin-nav__heading Admin
        //-     ul.side-nav
        //-       +navItem('#', 'Manage tours', 'map')
        //-       +navItem('#', 'Manage users', 'users')
        //-       +navItem('#', 'Manage reviews', 'star')
        //-       +navItem('#', 'Manage bookings', 'briefcase')

      .user-view__content
        block accountView
          